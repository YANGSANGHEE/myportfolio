import { PageSet } from '@/common/PageSet';

const PageFive = () => {
  return (
    <PageSet>
      <img src='/img/imsi.png' width={533} height={761} alt={'imsi'}></img>
    </PageSet>
  );
};

export default PageFive;
